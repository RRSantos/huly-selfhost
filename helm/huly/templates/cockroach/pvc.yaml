{{- if .Values.cockroach.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cockroach-data
  labels:
    {{- include "huly.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.cockroach.storageClassName }}
  storageClassName: {{ .Values.cockroach.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.cockroach.storage }}

---
{{- if .Values.cockroach.certs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cockroach-certs
  labels:
    {{- include "huly.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.cockroach.certs.storageClassName }}
  storageClassName: {{ .Values.cockroach.certs.storageClassName }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.cockroach.certs.storage }}
{{- end }}

{{- end }}
